hydra:
    output_subdir: .hydra
    run:
        dir: results/imdb/seed-${parameters.seed}/${now:%Y-%m-%d}/${now:%H-%M-%S}
    sweep:
        dir: results/imdb/seed-${parameters.seed}/${now:%Y-%m-%d}/${now:%H-%M-%S}
        subdir: ${hydra.job.num}
